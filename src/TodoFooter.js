import React, { Component} from 'react';
import PropTypes from 'prop-types';
import styles from './CSSModule.module.scss';

const TodoFooter = () => {
    return (
        <div className={styles.todo_footer}>hello</div>
    )
}

export default TodoFooter;